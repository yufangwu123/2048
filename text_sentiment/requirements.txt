# 默认装 CPU 版 PyTorch（体积小，笔记本够用）
# 如需 GPU，可改用：pip install torch --index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.66.0
scikit-learn>=1.3.0
pandas>=2.0.0
